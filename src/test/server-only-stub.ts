// Vitest stub for the `server-only` package, whose default entry throws when
// imported outside a React Server Components build. Aliased in vitest.config.ts
// so server modules (storage, DAL queries) can be unit-tested under Node.
export {};
