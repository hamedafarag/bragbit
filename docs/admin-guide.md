# Admin guide

For workspace **owners** and **admins** (organizations), plus the hosted **instance superadmin**.

> **Status:** stub — filled in Phase 2 (workspace administration) and Phase 10 (hosted superadmin).

- Workspace branding (name, logo, accent color)
- Members & invitations (invite, resend/revoke, change role, remove member = export-then-delete)
- Ownership transfer
- (Hosted) instance superadmin: workspaces / users / quotas — never brag content
