# BragBit — Enhancement Backlog (side plan)

A companion to [PLAN.md](../PLAN.md). PLAN tracks the forward feature **phases** (Phase 10 = hosted
multi-tenant, Phase 11 = v2). This is the cross-cutting backlog of **quality, tech-debt, testing,
security, performance, and polish** work surfaced during the v0.1.0 build and the post-release code
review (2026-06-16). Items here are independently pickable; they don't block a release on their own.

**Priority:** P1 (do soon) · P2 (should) · P3 (nice-to-have).
**Effort:** S (< ½ day) · M (½–2 days) · L (> 2 days).

## A. Code quality & tech debt

| ID           | Pri      | Effort | Enhancement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Why                                                                                                                                                                                  |
| ------------ | -------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ENH-CQ-01    | ✅ Fixed | S      | ~~Replace the `app/page.tsx` demo mockup (318 lines of hardcoded fake brags) with a redirect to `/dashboard` or a real landing page.~~ **Done 2026-06-16** — root `/` is a mode/session-aware redirect; the demo mockup is gone. (Row was stale; already in the Done ledger.)                                                                                                                                                                                                                                                                                                                                                                                            | The root `/` shows fabricated data to real users — the biggest smell in the codebase.                                                                                                |
| ENH-CQ-02    | ✅ Fixed | S      | ~~Bump `tsconfig` `target` ES2017 → ES2022.~~ **Done 2026-06-20** — now `ES2022` (`lib` was already `esnext`, so type globals are unchanged); no bundle change, since Next compiles via SWC from Browserslist, not the tsconfig `target`.                                                                                                                                                                                                                                                                                                                                                                                                                                | Stale create-next-app default; Node 22 / modern browsers support far newer output.                                                                                                   |
| ENH-CQ-03    | ✅ Fixed | M      | ~~Decompose the largest client components (`brag-editor` 288, `share-dialog` 265) into focused sub-components.~~ **Done 2026-06-20** — `brag-editor` 288→210, `share-dialog` 265→203; extracted field/sub-panel components and lifted pure `parseBragForm` + `formatAccessed` into unit-tested modules.                                                                                                                                                                                                                                                                                                                                                                  | Upper bound of comfortable size; smaller pieces read and test better.                                                                                                                |
| ENH-CQ-04    | ✅ Fixed | S      | ~~Move domain logic (the `beforeDelete` workspace-reaping) out of `lib/auth/index.ts` into a feature module.~~ **Done 2026-06-20** — extracted to `src/features/account/deletion.ts` (`cleanupUserStorage`); the `deleteUser` beforeDelete hook now just calls it (landed with the ENH-CQ-08 fix).                                                                                                                                                                                                                                                                                                                                                                       | The auth config object carries real business logic inline.                                                                                                                           |
| ENH-CQ-05    | ✅ Fixed | M      | ~~Create more bundle-budget headroom (now **398.37 / 400 kB**, measured 2026-06-20).~~ **Done 2026-06-21** — audited the bundle (no accidental bloat; markdown lazy, `@aws-sdk` server-only) and set the budget deliberately to **420 kB** (~21 kB / 5% headroom over the 398.52 kB footprint of necessary deps).                                                                                                                                                                                                                                                                                                                                                        | ~1.6 kB of slack now (was ~3.8 at the `/` cleanup; drifted as enhancements landed) — tight; audit client JS or set the limit deliberately.                                           |
| ENH-CQ-06    | ✅ Fixed | S      | ~~Skip the guard double-redirect — send a signed-in guard / `/admin` failure straight to `/dashboard` instead of `/` (which re-dispatches).~~ **Done 2026-06-20** — `requireRole` and the `/admin` layout now send insufficient-role callers straight to `/dashboard` (one 307, not `/admin → / → /dashboard`); e2e asserts the single redirect.                                                                                                                                                                                                                                                                                                                         | A non-admin hitting `/admin` now does `/admin → / → /dashboard` (two 307s) — harmless, but worth tidying when `/` gains the hosted-landing branch (Phase 10 / v1.1).                 |
| ENH-CQ-07 🐞 | ✅ Fixed | S      | ~~**BUG:** a signed-in but **workspace-less** session (e.g. a removed member who still has an account) ping-pongs `/dashboard → / → /dashboard` into `ERR_TOO_MANY_REDIRECTS`.~~ **Fixed 2026-06-19** — added a terminal `/no-workspace` page and pointed `requireWorkspace`'s two failure redirects at it (was `/`).                                                                                                                                                                                                                                                                                                                                                    | Found + fixed in org testing 2026-06-19 (TC-MEM-04). Access was always denied (no content leak); the bug was the infinite-loop failure mode. See Done ledger.                        |
| ENH-CQ-08 🐞 | ✅ Fixed | M      | ~~**BUG:** deleting a user account orphaned their uploaded attachment **files** in storage — the row cascade (user → … and a dropped sole-member org → documents → brags → attachments) removes the DB rows, but the stored objects were never deleted, unlike `deleteDocument` which purges them.~~ **Fixed 2026-06-20** — the `deleteUser` beforeDelete hook now calls `cleanupUserStorage` (`src/features/account/deletion.ts`), which purges the avatar + every orphaned attachment object (the user's own docs across workspaces, plus all docs in a dropped sole-member workspace) before the cascade. DB-gated regression test added to `object-cleanup.test.ts`. | Found in TC-ACCT-06 testing 2026-06-20. Storage-space leak only — orphaned files are unreachable (the file route 404s once the rows are gone), not a data exposure. See Done ledger. |

| ENH-CQ-09 🐞 | ✅ Fixed | S | ~~**BUG:** per-workspace white-labeling never reached any dialog or toast — on a workspace with a custom accent, the share dialog's primary button and the brag editor's focus rings rendered in the default `#e8590c`.~~ **Fixed 2026-07-16** — the accent is now published as a `:root` rule (`accentCss` / `<AccentStyle>`) instead of an inline style on a layout wrapper, so it reaches content rendered outside the tree. | Cosmetic only, but it silently broke a headline Phase-2 feature. Radix portals dialogs into `document.body` and the sonner `Toaster` mounts in the root layout — both outside any wrapper the accent could sit on. Invisible on a default-accent workspace, which is why the e2e org (no accent set) never caught it. See Done ledger. |

## B. Testing

| ID          | Pri      | Effort | Enhancement                                                                                                                                                                                                                                                                                                                 | Why                                                                                                 |
| ----------- | -------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| ENH-TEST-01 | ✅ Fixed | M      | ~~e2e for the core flow: capture → timeline → share (+ password) → export.~~ **Done 2026-06-20** — `tests/e2e/core-flow.spec.ts` drives the full journey (create doc → quick-add a win → timeline → share + password → logged-out unlock incl. wrong-password reject → Markdown export).                                    | Today only `admin-gating` + `home` exist; core flows had no e2e — the biggest gap. See Done ledger. |
| ENH-TEST-02 | ✅ Fixed | M      | ~~e2e for the setup wizard (both modes) + invitation accept.~~ **Done 2026-06-20** — `tests/e2e/invitation.spec.ts` (real sign-up → accept → membership; CI gained a Mailpit service) and `tests/e2e-setup/setup.spec.ts` (first-run wizard, both modes, via a dedicated `playwright.setup.config.ts` + isolated empty DB). | The first-run and onboarding paths are untested end to end. See Done ledger.                        |
| ENH-TEST-03 | ✅ Fixed | S      | ~~A documented `pnpm test:db` that runs the DB-gated suite against the dev database locally.~~ **Done 2026-06-20** — `pnpm test:db` (+ `test:db:coverage`), documented in CONTRIBUTING.                                                                                                                                     | They skip by default; easy local coverage would catch regressions before CI.                        |
| ENH-TEST-04 | ✅ Fixed | S      | ~~Add a QA checkbox to the PR template pointing at the [manual test plan](testing.md).~~ **Done 2026-06-20** — PR template links the manual test plan as a QA checkbox.                                                                                                                                                     | Prompts a manual pass on user-facing PRs.                                                           |
| ENH-TEST-05 | ✅ Fixed | M      | ~~Coverage reporting + a soft threshold in CI.~~ **Done 2026-06-20** — V8 coverage wired into the CI `database` job with a **ratchet** threshold in `vitest.config.ts` (fails on regression, not just soft). Baseline locked at the 2026-06-20 numbers.                                                                     | Visibility into what's actually exercised.                                                          |

## C. Security

| ID            | Pri      | Effort | Enhancement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Why                                                                                                                                                                                                            |
| ------------- | -------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ENH-SEC-01    | ✅ Fixed | M      | ~~Add a `script-src` Content-Security-Policy with per-request nonces.~~ **Done 2026-06-20** — per-request nonce + `strict-dynamic` emitted by `src/proxy.ts` (Next 16 Proxy); the framing/base/plugin baseline moved there too (one CSP source of truth); the root layout opts into dynamic rendering so the nonce applies app-wide.                                                                                                                                                                                                              | The CSP intentionally omits `script-src` today; nonces close the XSS gap (needs middleware).                                                                                                                   |
| ENH-SEC-02    | P2       | M      | Shared rate-limit store (Better Auth `secondaryStorage` / Redis-backed `lib/rate-limit`).                                                                                                                                                                                                                                                                                                                                                                                                                                                         | The in-memory limiter is single-process; multi-instance needs a shared store (Phase 10 prereq).                                                                                                                |
| ENH-SEC-03    | ✅ Fixed | S      | ~~Trusted-proxy client-IP configuration.~~ **Done 2026-06-20** — `TRUSTED_PROXY_IP_HEADER` points Better Auth's IP detection at a non-standard proxy header; the default `x-forwarded-for` already works behind the reference proxy.                                                                                                                                                                                                                                                                                                              | Accurate per-client rate-limiting behind the reference reverse proxy.                                                                                                                                          |
| ENH-SEC-04    | ✅ Fixed | S      | ~~`pnpm audit` in CI with an allowlist for the known dev-tool advisories.~~ **Done 2026-06-20** — CI gates on dependency advisories; the esbuild dev-server advisory (GHSA-67mh-4wv8-2f99, build-time only) is accepted with a documented reason. **Re-tooled 2026-07-16** — npm retired the audit endpoints `pnpm audit` calls (410 on every branch), so the gate is now `osv-scanner` over `pnpm-lock.yaml` with `osv-scanner.toml`. The check immediately caught a real HIGH prod advisory in nodemailer (≤9.0.0) → fixed by bumping to 9.0.1. | Catches new prod-dependency advisories without failing on the accepted build-tool ones. See Done ledger.                                                                                                       |
| ENH-SEC-05    | ✅ Fixed | S      | A security-review pass + a dependency-update cadence before the repo goes public. **Review pass done 2026-06-20** (3-surface audit: auth/authz, XSS/injection/web, data/files/tokens) — findings logged as ENH-SEC-07/08; a flagged "OAuth email-collision takeover" was verified **not exploitable** (GitHub/Google only return an attacker-owned verified email). Dependency-cadence part **done** via ENH-INFRA-02 (weekly Dependabot) — **both parts complete 2026-06-20.**                                                                   | Career data is sensitive; a deliberate review before public exposure.                                                                                                                                          |
| ENH-SEC-06 🐞 | ✅ Fixed | S      | ~~**BUG:** in the private (invitation-only) modes, `POST /api/auth/sign-up/email` is open — anyone reachable by the instance can create an account.~~ **Fixed 2026-06-19** — the catch-all auth route rejects the public `sign-up/email` path in non-hosted modes. `emailAndPassword.disableSignUp` couldn't be used (it's inside the sign-up handler, so it also blocks the setup wizard + invite-accept, which call `auth.api.signUpEmail` directly).                                                                                           | Found + fixed 2026-06-19. No data exposure (a self-signed-up user has no membership → `/no-workspace`), but it closes the "no public signup" gap + the verification-email relay-abuse vector. See Done ledger. |
| ENH-SEC-07    | ✅ Fixed | S      | ~~Share-password hashing uses `@node-rs/argon2` defaults (memoryCost 4 MiB, t=3) — below the OWASP Argon2id minimum (~19 MiB).~~ **Done 2026-06-20** — `src/features/share/actions.ts` now hashes with `{ memoryCost: 19456, timeCost: 2, parallelism: 1 }`; `verify` reads params from the encoded hash so existing hashes still validate.                                                                                                                                                                                                       | Cheaper offline cracking if the DB/backups leak. Found in the 2026-06-20 security review. See Done ledger.                                                                                                     |
| ENH-SEC-08    | ✅ Fixed | S      | ~~Operator-secret hardening: `BETTER_AUTH_SECRET` `.min(1)`→`.min(32)`; useSecureCookies; trustedOrigins.~~ **Done 2026-06-20** — `src/lib/env.ts` now requires a ≥32-char secret (CI placeholders + test docs updated); Better Auth `trustedOrigins: [APP_URL]` set. useSecureCookies intentionally **not** forced (Better Auth derives `Secure` from the `APP_URL` scheme, which correctly supports plain-http self-hosts) — instead `.env.example` documents using https in prod.                                                              | Prevents footguns that weaken session/cookie/HMAC security on a misconfigured self-host. Found in the 2026-06-20 security review. See Done ledger.                                                             |

## D. Performance

| ID          | Pri      | Effort | Enhancement                                                                                                                                                                                                                                                                                              | Why                                                                                                  |
| ----------- | -------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| ENH-PERF-01 | P2       | L      | Timeline cursor pagination (month-windowed).                                                                                                                                                                                                                                                             | Deferred from Phase 5; the `(document_id, date)` index already exists. Matters at hundreds of brags. |
| ENH-PERF-02 | ✅ Fixed | M      | ~~`sharp`-downscaled thumbnails for image attachments and avatars.~~ **Done 2026-06-21** — the authorizing `/api/files` route serves a downscaled webp on an allowlisted `?w=` (avatars, attachments, logos), on-demand with graceful fallback; render sites request it via `thumbUrl`. See Done ledger. | Deferred Phase 4 refinement; the timeline currently serves full-res through the authorizing route.   |
| ENH-PERF-03 | ✅ Fixed | S      | ~~Review / enforce the Lighthouse CI budgets.~~ **Done 2026-06-20** — budgets now **fail** CI (perf/CLS/LCP/TBT as errors with CI-noise headroom), median-of-3 runs.                                                                                                                                     | Keep Core Web Vitals honest as the app grows.                                                        |

## E. Infrastructure, CI & tooling

| ID           | Pri      | Effort | Enhancement                                                                                                                                                                                                                                                                                      | Why                                                                                                        |
| ------------ | -------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| ENH-INFRA-01 | ✅ Fixed | S      | ~~Bump GitHub Actions off Node 20 (`actions/checkout@v4`, `setup-node@v4`, etc.).~~ **Done 2026-06-20** — `checkout@v4→v5`, `setup-node@v4→v5`, `pnpm/action-setup@v4→v6` (all run on node24).                                                                                                   | Node 20 is being force-migrated to 24 on the runners; the deprecation warning was firing. See Done ledger. |
| ENH-INFRA-02 | ✅ Fixed | S      | ~~Add Renovate or Dependabot.~~ **Done 2026-06-20** — `.github/dependabot.yml`: weekly grouped updates for npm (pnpm), GitHub Actions, and the Docker base image, with Conventional-Commit prefixes.                                                                                             | Automated, reviewable dependency updates.                                                                  |
| ENH-INFRA-03 | ✅ Fixed | S      | ~~A dedicated `/api/health` endpoint for the container health check.~~ **Done 2026-06-20** — `GET /api/health` runs a `select 1` (200 `{status:"ok"}` / 503 on DB failure); the compose healthcheck now targets it. Unit-tested (both branches).                                                 | Cleaner than hitting `/` (which renders the app).                                                          |
| ENH-INFRA-04 | P3       | L      | Optional headless-Chromium PDF service (`browserless`).                                                                                                                                                                                                                                          | Server-side PDF as an add-on; browser print is the deliberate v1 path.                                     |
| ENH-INFRA-05 | ✅ Fixed | S      | ~~Make `seed-demo` runnable inside the container, or document the local-only constraint.~~ **Done 2026-06-20** — documented the local-only constraint (the slim image omits the script's dev deps; run from a checkout with `DATABASE_URL` at the instance DB) in the Docker self-hosting guide. | The script needs `dotenv` + `better-auth/crypto`, which aren't in the slim image.                          |

## F. UX & product polish

| ID        | Pri      | Effort | Enhancement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Why                                                                          |
| --------- | -------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| ENH-UX-01 | ✅ Fixed | L      | ~~Dark mode — a designed ink-on-dark variant (not a mechanical invert).~~ **Done 2026-06-21** — a `.dark` CSS-variable palette (warm layered surfaces, hand-lightened category colors) with a header sun/moon toggle; cookie-driven SSR (no flash for returning users) + a client `ThemeInit` for the OS preference. See Done ledger.                                                                                                                                                                                                                                                                                                                                        | Deferred from Phase 0; a real post-v1 enhancement.                           |
| ENH-UX-02 | ✅ Fixed | M      | ~~Accessibility pass (keyboard nav, focus management, ARIA on dialogs/forms, contrast).~~ **Done 2026-06-21** — audited (strong baseline: Radix dialogs, focus rings, alt text, aria-labels, landmarks); added a skip-to-content link and fixed contrast — darkened `ink-faint` to AA (~4.8:1) and made the accent-button text auto-pick white/ink by luminance.                                                                                                                                                                                                                                                                                                             | No a11y audit has been done; important for an inclusive OSS tool.            |
| ENH-UX-03 | ✅ Fixed | S      | ~~Reduce the `no-img-element` eslint-disables where `next/image` can serve.~~ **Done 2026-06-21 (reviewed)** — all 9 disables are justified: avatars/attachments stream through the session-gated `/api/files` route (the optimizer can't fetch them), and optimizing the public logos needs `sharp` (ENH-PERF-02, deferred) — without it next/image is unoptimized passthrough. PERF-02 (2026-06-21) then made these images load downscaled via the route's `?w=` webp thumbnail, so the perf goal is met without next/image; the disables stay (the optimizer still can't reach the gated routes, and logos use the same `?w=` path) with their comments updated to match. | 10 justified disables today; some non-authorizing images could be optimized. |
| ENH-UX-04 | ✅ Fixed | M      | ~~Entry content templates (action-verb scaffolds) to beat the blank page.~~ **Done 2026-06-21** — a row of six action-verb chips under the quick-add box (Shipped a project · Fixed a critical issue · Led an initiative · Mentored a teammate · Improved a process · Earned recognition); each opens the editor pre-filled with the matching category (+ status where unambiguous) and a Markdown description scaffold, reusing the existing `initial`/remount-on-open path. See Done ledger.                                                                                                                                                                               | Pulls a PLAN §11 item earlier; cheap capture-friction win.                   |
| ENH-UX-05 | ✅ Fixed | M      | ~~Streak + GitHub-style activity heatmap on the dashboard.~~ **Done 2026-06-21** — a year-long, accent-tinted heatmap + week streak on the dashboard (`features/dashboard`), from a pure `buildActivity` model over a workspace-scoped per-day win aggregation. See Done ledger.                                                                                                                                                                                                                                                                                                                                                                                             | Drives logging cadence; pairs with the weekly reminders (PLAN §11).          |

## G. Tracked carryover

| ID        | Pri      | Effort | Enhancement                                                                                                       | Why                                                                          |
| --------- | -------- | ------ | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| ENH-CO-01 | ✅ Fixed | M      | ~~Member-removal export bundle (Markdown + JSON + attachments handed to a removed member).~~ **Done 2026-06-19.** | Phase 2 carryover, built on `features/export`. See Done ledger for delivery. |

## Tracked elsewhere (big phases — see PLAN.md)

- **Phase 10 — hosted multi-tenant (v1.1):** open signup, user-created orgs, the `/super` console,
  quota/abuse controls, the data-isolation test suite. See [PLAN.md §10](../PLAN.md).
- **Phase 11 — v2 backlog:** REST API + personal access tokens (and the
  [MCP connector](specs/mcp-connector.md) for in-assistant capture from Claude Desktop), a companion
  CLI (git-history brag extraction), SSO (OIDC/SAML), optional BYO-key AI, curated sharing,
  Markdown/Notion import. See [PLAN.md §11](../PLAN.md).

## Recommended order (highest leverage first)

Only three rows are still open; everything else in A–G is in the Done ledger.

1. **ENH-SEC-02** — the shared rate-limit store. The in-memory limiter is single-process, so this
   gates any multi-instance or hosted deployment. _M_
2. **ENH-PERF-01** — timeline cursor pagination, once a workspace reaches hundreds of brags. _L_
3. **ENH-INFRA-04** — the optional headless-Chromium PDF service; browser print stays the
   deliberate default, so this is purely additive. _P3, L_

## Done

| ID           | Done       | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ENH-CQ-01    | 2026-06-16 | Root `/` is now a mode/session-aware redirect; the demo mockup is gone (−2.5 kB).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ENH-CQ-07    | 2026-06-19 | Removed-member redirect loop. New terminal `src/app/no-workspace/page.tsx` + `requireWorkspace` now redirects no-workspace/no-membership callers there (not `/`). A workspace-less session lands on a "No workspace access" page with sign-out; valid users hitting it bounce to `/dashboard`. Verified: removed member sign-in → `/no-workspace` (no loop), sign-out recovers. typecheck + lint clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ENH-SEC-06   | 2026-06-19 | Closed the open public email/password sign-up in private (invitation-only) modes. The catch-all auth route (`src/app/api/auth/[...all]/route.ts`) returns 403 for the public `sign-up/email` path when `!isHosted()`; the setup wizard + invite-accept use `auth.api.signUpEmail` server-side and bypass the route, so they're unaffected. Verified live: public POST → 403 (no user); a fresh org setup + an invite-accept both still create their users. typecheck + lint clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ENH-CO-01    | 2026-06-19 | Member-removal export bundle. `removeMember` now emails the removed member their full data (best-effort, after the purge) — a branded `MemberRemoved` mail with `bragbit-data.json` (every doc + brag, private included), a combined `bragbit-wins.md`, and the actual attachment files up to a 20 MB cap (oversized ones listed in the JSON). New `features/workspace/offboard.ts` (+ pure `offboard-bundle.ts` with a unit-tested size cap), `emails/member-removed.tsx`, and a `sendEmail` `attachments` passthrough. Live-verified against Mailpit: email + 3 attachments (json/md/pdf), JSON has 5 brags incl. private. typecheck + lint + tests green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ENH-CQ-08    | 2026-06-20 | Account deletion no longer orphans attachment files. The `deleteUser` beforeDelete hook (`src/lib/auth/index.ts`) now calls `cleanupUserStorage` (`src/features/account/deletion.ts`): it collects every attachment object the deletion will orphan — the user's own documents across all workspaces, plus all documents in a sole-member workspace being dropped — and best-effort `getStorage().delete()`s them (alongside the avatar) before the row cascade. DB-gated regression test in `object-cleanup.test.ts` asserts the objects + avatar are purged and the sole-member workspace is dropped. Found in TC-ACCT-06 testing; storage-leak only (orphans were already unreachable). typecheck + lint + full test suite green (new test verified against the dev DB).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ENH-CQ-04    | 2026-06-20 | Moved the `deleteUser` beforeDelete domain logic out of `lib/auth/index.ts` into `src/features/account/deletion.ts` (`cleanupUserStorage`); the auth config now just calls it. Landed with ENH-CQ-08.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ENH-TEST-01  | 2026-06-20 | Core-flow e2e. New `tests/e2e/core-flow.spec.ts` drives the product's main journey via the UI as the seeded e2e owner: create a document → quick-add a win → assert it on the timeline → open the Share dialog, create a link, set a password → in a fresh (logged-out) browser context open the share, get rejected with a wrong password, then unlock with the right one and see the win → export to Markdown and assert the download contains the document title + win. Reuses the `admin-gating` UI sign-in pattern; runs green in private-solo (the CI default mode). typecheck + lint + full e2e suite (4 tests) green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ENH-INFRA-01 | 2026-06-20 | Moved CI off the deprecated Node 20 action runtime (GitHub is force-migrating Node-20 actions to Node 24; the warning was firing on every run). Bumped all 6 jobs in `.github/workflows/ci.yml`: `actions/checkout@v4→v5`, `actions/setup-node@v4→v5`, `pnpm/action-setup@v4→v6` — each confirmed `runs.using: node24` via the GitHub API. Left `node-version: 22` (the app's runtime, matching the `node:22` Dockerfile — unrelated to the action-runtime deprecation); `lychee-action`/`lighthouse-ci-action` weren't flagged (Docker-based / already node24). pnpm version still resolves from `packageManager: pnpm@10.30.3`, so the major bump doesn't change it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ENH-SEC-07   | 2026-06-20 | Raised the share-password argon2 work factor to the OWASP Argon2id minimum. `setSharePassword` (`src/features/share/actions.ts`) now hashes with `{ memoryCost: 19456, timeCost: 2, parallelism: 1 }` instead of the 4 MiB `@node-rs/argon2` default; `verify` reads the cost params back from the encoded hash, so previously-set passwords keep working. Verified via the DB-gated `share/security.test.ts` and the core-flow e2e (set + unlock through the UI).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ENH-SEC-08   | 2026-06-20 | Operator-secret hardening from the security review. `BETTER_AUTH_SECRET` validation raised to `.min(32)` (`src/lib/env.ts`) — updated the 4 CI placeholder secrets and the two test-run doc comments to ≥32 chars to match. Set Better Auth `trustedOrigins: [APP_URL]` so legitimate same-origin/proxied requests pass the CSRF Origin check. Did NOT force `useSecureCookies`: Better Auth already derives the cookie `Secure` flag from the `APP_URL` scheme (correct for both https and plain-http self-hosts), so instead `.env.example` documents using https in production. typecheck + lint + full e2e (4) + DB-gated security tests (11) green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ENH-SEC-04   | 2026-06-20 | Added `pnpm audit --prod` to the CI `verify` job, with the accepted dev-tool advisory allowlisted in `package.json` (`pnpm.auditConfig.ignoreGhsas: ["GHSA-67mh-4wv8-2f99"]` — the esbuild dev-server CORS issue, which only reaches the tree via `better-auth → drizzle-kit → @esbuild-kit` and never runs in the standalone prod image, since `scripts/migrate.mjs` uses the drizzle-orm runtime migrator). The new check immediately caught a real HIGH advisory in nodemailer (≤9.0.0: file-read + SSRF in the delivered message, GHSA-p6gq-j5cr-w38f) — fixed by bumping nodemailer 8→9.0.1 (kept @types/nodemailer@8; createTransport/sendMail API unchanged). `pnpm audit --prod` now exits clean; typecheck + build + tests green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ENH-INFRA-02 | 2026-06-20 | Added `.github/dependabot.yml`: weekly grouped dependency updates for npm (pnpm, root), GitHub Actions, and the Docker base image. Minor/patch npm bumps group into one PR (majors stay separate); commit prefixes are Conventional (`chore`/`ci`) to satisfy commitlint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ENH-TEST-03  | 2026-06-20 | Added `pnpm test:db` (and `test:db:coverage`) — runs the full suite incl. the DB-gated server-action tests against the dev DB (`:5433`), documented in CONTRIBUTING. Closes the "skipped by default, no easy local run" gap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ENH-TEST-04  | 2026-06-20 | PR template (`.github/PULL_REQUEST_TEMPLATE.md`) now has a QA checkbox linking the manual test plan (shipped with the public-hygiene batch; backlog row reconciled).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ENH-TEST-05  | 2026-06-20 | Coverage reporting + a ratcheting CI gate. Added `@vitest/coverage-v8` (pinned to vitest 4.1.8) and a `coverage` block in `vitest.config.ts` (v8, lcov+json-summary+text-summary, `all:true`, schema/migrations/layout excluded). CI measures coverage in the `database` job (`pnpm test:coverage`, where DB-gated tests run so server actions count) and uploads the report. Thresholds are a ratchet locked to the 2026-06-20 baseline — global lines/stmts/funcs 13 / branches 9, with stricter floors for `src/lib/**/*.ts` (lines 37) and `src/features/**/*.ts` (lines 32). Enforcement verified (an impossible floor fails the run); the gate climbs as Layer 2/3 add tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ENH-TEST-02  | 2026-06-20 | End-to-end coverage for onboarding. `tests/e2e/invitation.spec.ts` drives the real accept flow (logged-out invitee → Better Auth sign-up → accept → asserts membership by polling the DB; the CI e2e job gained a throwaway Mailpit SMTP service so the verification email sends). The setup wizard needs an EMPTY, mode-specific instance (it closes once a workspace exists, and `INSTANCE_MODE` is fixed at start), so it runs under a dedicated `playwright.setup.config.ts` against an isolated DB (`scripts/e2e-setup-db.mjs` provisions it; the spec truncates per attempt) on its own `next dev` port — invoked once per private mode by `pnpm test:e2e:setup` (sequential, so only one `next dev` is ever live). Verifies the wizard creates a personal (solo) vs organization (org) workspace and that the member surface exists only for orgs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ENH-INFRA-03 | 2026-06-20 | Added a dedicated `/api/health` route (`src/app/api/health/route.ts`): `GET` runs a `select 1` through `db.execute` and returns 200 `{status:"ok"}` when Postgres is reachable, 503 `{status:"error"}` otherwise (marked `dynamic = "force-dynamic"` so it's never cached). The docker-compose app healthcheck now probes `/api/health` (`r.ok`) instead of `/` — a real app+DB readiness check independent of setup state. Both branches unit-tested by mocking `@/lib/db` (`route.test.ts`); coverage rose to lines 24.68 / stmts 24.81 / fns 22.16 (integer ratchet floors unchanged — no whole percent crossed). Verified live (200 `{"status":"ok"}`); typecheck + lint + build (route registers as `ƒ`) + size 398.37 kB green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ENH-CQ-06    | 2026-06-20 | Skipped the `/admin` guard double-redirect. `requireRole` (`src/lib/auth/guards.ts`) and the `/admin` layout (`src/app/(app)/admin/layout.tsx`) now redirect an insufficient-role caller straight to `/dashboard` instead of `/`, which would only re-dispatch a signed-in caller back to `/dashboard` anyway — so a non-admin hitting `/admin` is one 307, not the `/admin → / → /dashboard` double hop (mirrors the ENH-CQ-07 `/no-workspace` reasoning). `tests/e2e/admin-gating.spec.ts` updated to assert the single direct redirect (a `maxRedirects: 0` GET returns 307 → `/dashboard`). typecheck + lint + e2e (admin-gating, 2) green; coverage unchanged (≥ floors); size flat (398.37 kB).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ENH-SEC-01   | 2026-06-20 | Added a `script-src` CSP with per-request nonces. New `src/proxy.ts` (Next 16 renamed Middleware → Proxy) mints a fresh nonce per matched request and emits `script-src 'self' 'nonce-…' 'strict-dynamic'` (plus `'unsafe-eval'` only in dev), alongside the framing/base/plugin baseline that used to be a static header in `next.config.ts` — so the CSP now has one source of truth. The root layout calls `connection()` to force dynamic rendering so the nonce applies app-wide (incl. the otherwise-static not-found page); Next auto-applies the nonce to its own scripts. New `src/proxy.test.ts` (buildCsp + fresh-nonce-per-request). Verified: full e2e (6) green under the proxy; a production build serves the strict CSP (no `unsafe-eval`) with the nonce on all 23 script tags and **zero console CSP violations** in a real browser; typecheck + lint + size (398.52 kB) ok; ratchet up (global statements 24→25, lines 24→25). Lighthouse runs in CI on push.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ENH-UX-03    | 2026-06-21 | next/image cleanup — reviewed; no safe conversion today. Investigated all 9 `@next/next/no-img-element` disables: avatars + attachments stream through the **session-gated** `/api/files` route (next/image's optimizer can't fetch an authorizing stream), and the public `branding/` logos would need `sharp` for the production optimizer — not a dependency (adding it is ENH-PERF-02, deferred). Without sharp, next/image is unoptimized passthrough (no benefit + an optimizer round-trip), so plain `<img>` is correct for now. Updated all 9 disable comments to record the accurate rationale (sharp/PERF-02 + the gated routes). The logo conversion will land with PERF-02.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ENH-UX-02    | 2026-06-21 | Accessibility pass. Audited the interactive surfaces — strong baseline already (Radix dialogs handle focus-trap/ESC/ARIA, buttons have `focus-visible` rings, all images have alt, icon buttons have aria-labels, header/main/nav landmarks, toasts in a live region). Added a **skip-to-content** link to the app shell (`src/app/(app)/layout.tsx`) targeting `<main id="main-content" tabIndex={-1}>` — verified in-browser (Tab reveals it, Enter moves focus to main). Fixed contrast: an objective audit found the faint metadata (`--color-ink-faint` #948a78, ~3.0–3.4:1 on small text) and the primary button (white on the default accent, ~4.0:1) below WCAG AA — darkened `ink-faint` to #726a58 (~4.8–5.3:1) and made `accentVars()` (`src/lib/utils.ts`) pick the button text (white vs ink) by the accent's luminance, so warm accents (orange ~4.6, gold ~5.1) now pass and any accent gets its best-contrast text. New unit tests for `accentVars`. typecheck + lint + full e2e (6) + coverage (floors ratcheted: global fn 22→23, lib to 38/27/36/42) + size (398.7 kB) green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ENH-CQ-05    | 2026-06-21 | Bundle-budget headroom. Audited the client bundle (`pnpm build` + per-chunk gzip): no accidental bloat — `@aws-sdk/client-s3` is `server-only` (never shipped), react-markdown ships only as a lazy `next/dynamic` chunk for the editor preview (`markdown-field.tsx`, `ssr:false`), Radix uses tree-shakeable namespace imports, lucide is per-icon. The 398.52 kB total conservatively sums ALL chunks (it even counts the lazy markdown-preview chunk that isn't first-load), composed of necessary deps (React, react-markdown for safe live-preview, Radix, Sonner, zod). No cheap trim available, so set the `pnpm size` budget deliberately to 420 kB (`.size-limit.json`) — ~21 kB / 5% headroom over the natural footprint: room for minor additions without a budget scramble, while still catching a heavy dep accidentally pulled client-side. A deeper trim would need a server-rendered editor preview (UX/security trade-off) — noted as a future option.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ENH-SEC-03   | 2026-06-20 | Trusted-proxy client-IP. Better Auth already reads the real client IP from `X-Forwarded-For` by default (`getIp`), so per-client auth rate-limiting is accurate behind the reference reverse proxy. Added `TRUSTED_PROXY_IP_HEADER` (`src/lib/env.ts`) wired into Better Auth's `advanced.ipAddress.ipAddressHeaders` (`src/lib/auth/index.ts`) so operators whose proxy uses a different header (Cloudflare `cf-connecting-ip`, `x-real-ip`, …) can point it there. Documented in configuration.md / architecture.md / `.env.example`, with the caveat to trust the header only when a proxy sets it. typecheck + lint clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ENH-SEC-05   | 2026-06-20 | Pre-public security review + dependency cadence — both parts complete. The 3-surface review pass shipped 2026-06-20 (findings became ENH-SEC-06/07/08; the OAuth email-collision concern was verified not exploitable), and the dependency-update cadence is the weekly Dependabot config (ENH-INFRA-02). Ledger reconciled.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ENH-PERF-03  | 2026-06-20 | Reviewed + enforced the Lighthouse CI budgets (`lighthouserc.json`). The assertions were warn-only (never failed CI); now performance (≥ 0.85), CLS (≤ 0.1), LCP (≤ 3000 ms), and TBT (≤ 500 ms) are errors that fail the `lighthouse` job, over `numberOfRuns: 3` (median) for stability — headroom left below typical values so normal CI variance doesn't flake, only real regressions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ENH-INFRA-05 | 2026-06-20 | Documented the `seed-demo` local-only constraint. The slim production image deliberately omits the script's dev deps (`dotenv`, `better-auth/crypto`), so it can't run via `docker compose exec`; the Docker self-hosting guide now says to run it from a local checkout with `DATABASE_URL` pointed at the instance database.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ENH-CQ-03    | 2026-06-20 | Decomposed the two largest client components. `brag-editor.tsx` 288→210 (extracted `BragMetaFields` — date/category/status — and `BragAttributionFields` — collaborators/attribution) and `share-dialog.tsx` 265→203 (extracted `ShareLinkRow` and `SharePasswordPanel`). Lifted the pure helpers `parseBragForm` (`src/features/brag/form.ts`) and `formatAccessed` (`src/features/share/format.ts`) into their own modules with new unit tests (9 cases) — cheap real coverage that ratcheted the floors up (global branches 16→17; `src/features/**/*.ts` branches 58→60, functions 62→63, lines 66→67). DOM contract preserved — `core-flow.spec.ts` (share → password → unlock → export) green; typecheck + lint + `pnpm size` (398.52 kB) ok. Full jsdom component tests were deferred here, then added 2026-06-21 (a React Testing Library + jsdom harness, `src/test/dom.ts`, now render-tests these sub-components and others).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ENH-UX-01    | 2026-06-21 | Dark mode — a designed warm "ink-on-dark" logbook (not a mechanical invert). All color flows through CSS variables, so a single `.dark` block in `globals.css` re-themes the app: warm near-black layered surfaces, a warm off-white ink, hand-lightened category colors, and dark dot-grid/wash/shadow overrides (the per-workspace accent + `accentVars()` stay theme-independent). Theme is a class on `<html>`: a `theme` cookie drives the SSR class (`themeClass`, src/lib/theme.ts) for no-flash on return visits; a client `ThemeInit` applies the OS preference on a first visit, and a header sun/moon `ThemeToggle` flips the class + cookie. Chose `ThemeInit` over a pre-paint inline script because that script can't carry the per-request CSP nonce (ENH-SEC-01) cleanly under React 19 — it caused a hydration mismatch; the only trade-off is a possible brief flash for a first-time _system-dark_ visitor. Pure `themeClass`/`resolveTheme` + both components unit-tested at 100% (`theme.test.ts`, `theme-init.test.tsx`, `theme-toggle.test.tsx`); e2e (`dark-mode.spec.ts`: toggle flips + persists through a reload). Live-verified in-browser (light + dark; toggle + cookie persistence; OS-preference first load; zero console/CSP errors). Ratchet: global branches 25→26, functions 35→36; `src/lib/**/*.ts` 41/30/40/45 → 43/33/42/47. typecheck + lint + format green.                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ENH-PERF-02  | 2026-06-21 | `sharp`-downscaled thumbnails. The authorizing `/api/files` route serves a downscaled **webp** when an allowlisted `?w=` width is requested (`src/lib/image.ts`: `thumbnail` via sharp — no upscaling, EXIF-rotated; `parseThumbWidth` gates the width to 64/192/400/800); avatars, attachment previews, and logos request it via `thumbUrl` (`src/lib/utils.ts`). On-demand — no upload changes, no stored copies, no backfill; an off-list width or non-image serves the full object, and a sharp failure falls back to the original. The print-page logo stays full-res for print quality. Unit-tested (`image.test.ts` 100%, incl. resize + webp + no-upscale; `thumbUrl` in `utils.test.ts`); curl-verified end-to-end (1200×900 PNG → `?w=64` webp 64×48, ~186× smaller; off-list + a degenerate image both fall back to the original). `sharp` is server-only (no client bundle; `pnpm size` unchanged). Ratchet: `src/lib/**/*.ts` 38/27/36/42 → 41/30/40/45 (global held). Also satisfies ENH-UX-03's perf intent — logos are now optimized without next/image. typecheck + lint + format + DB-gated coverage green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ENH-UX-05    | 2026-06-21 | Dashboard activity heatmap + streak. New `features/dashboard`: the pure `buildActivity` model (`activity.ts`) turns per-day win counts into a 52-week Sunday-aligned grid (intensity levels 0–4), month labels, a current **week streak** (lenient on the in-progress current week), the longest streak, and totals — all from an injected `today`, so it's fully unit-tested (`activity.test.ts`, 100%). A workspace-scoped `getActivityCounts` query (DB-gated test, 100%) aggregates the caller's wins per day across their documents (owner-scoped; private included, since it's the owner's own view). The server-rendered `ActivityHeatmap` (no client JS) paints accent-tinted cells with per-day tooltips and a single `role="img"` summary for screen readers (jsdom render tests, 100%); the dashboard shows it only once there's a win. Live-verified on the demo workspace (5 wins across the year; correct grid, month labels, and a correctly-lapsed 0 streak) + e2e (`dashboard-activity.spec.ts`: log a win today → the panel shows a non-zero streak). typecheck + lint + format + DB-gated coverage (ratchet bumped — global 31→34, `src/features/**/*.ts` 65/60/63/67 → 68/62/65/69) + size (server-only; unchanged) green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ENH-UX-04    | 2026-06-21 | Entry templates (action-verb scaffolds). Added a `TemplateBar` (`src/features/brag/components/template-bar.tsx`) beneath the quick-add box on the document page — a "START FROM" row of six chips, each a category-dotted trigger that opens the full `BragEditor` pre-filled from a template. Reuses the editor's existing prefill path (the dialog remounts on open and reads `initial`, exactly as edit mode does), so no editor changes were needed; the chip carries no date, so there's no SSR/client skew. New pure `src/features/brag/templates.ts` holds the six `BRAG_TEMPLATES` (category + optional status + a Markdown description scaffold of the genre prompts) and `templateToInitial(template, today)`, which builds the `BragFormValues` with empty defaults and the injected date — unit-tested (`templates.test.ts`, 5 cases incl. every template parsing cleanly through `bragSchema` once a title is added) at 100% lines/branches/functions. Live-verified against the demo workspace: all six chips render; "Shipped a project" opens "Log a win" with category=Shipped work, status=Shipped, today's date, and the scaffold seeded (title/impact left blank); a status-less template ("Led an initiative") leaves status empty — both `status ?? ""` branches confirmed in-browser; zero console/CSP violations. typecheck + lint + prettier + full DB-gated coverage (ratchet held — global 25.7/17.78/23.36/25.62, `src/features/**/*.ts` floors unchanged) + `pnpm size` (399.27 kB ≤ 420) green.                                                                                                                                                                                                                                                                                                                               |
| ENH-CQ-02    | 2026-06-20 | Bumped `tsconfig.json` `target` ES2017 → ES2022 (`lib` was already `esnext`, so available type globals are unchanged; the one semantic shift — `useDefineForClassFields` defaulting to `true` — is harmless here: the two storage classes (`S3Storage`, `LocalDiskStorage`) declare no class-field initializers and have no inheritance). No `src`/test changes. Verified: typecheck clean, `pnpm build` succeeds, fast suite (44) + the DB-gated coverage suite green at/above the ratchet floors, and `pnpm size` is identical to the ES2017 build (398.37 kB ≤ 400 kB) — confirming Next ships SWC output targeted from Browserslist, not the tsconfig `target`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ENH-CQ-09    | 2026-07-16 | Dialogs and toasts ignored the workspace accent. `accentVars` was applied as an inline `style` on a layout wrapper, so `--primary` / `--ring` / `--primary-foreground` only cascaded to that subtree — but Radix portals `DialogContent` into `document.body`, and the sonner `Toaster` (whose action button is `bg-primary`) mounts in the root layout. Both sit outside every wrapper, so on a white-labeled workspace they fell back to the `:root` defaults: measured `--primary: #e8590c` inside the share dialog and the brag editor against a `#5c8a58` workspace, the share button painting `rgb(232, 89, 12)`. The toaster is why a portal-container or wrapper-scoping fix couldn't work — nothing can wrap it. Added `accentCss` (`src/lib/utils.ts`) + `<AccentStyle>` (`src/components/shared/accent-style.tsx`), emitting the accent as a `:root` rule; `:root` is inherited by `body`, so portalled content is branded. Applied to all four page-level branded surfaces (the app shell, auth, share-chrome, print); `branding-form` keeps element-scoped `accentVars` for its live preview swatch, which still correctly overrides `:root` for its own subtree (verified). Why it hid for so long: the e2e org had no `accent_color`, so the default palette applied and the bug was undetectable — `global-setup` now brands it `#4338ca` and `tests/e2e/branding.spec.ts` asserts the accent reaches the shell, the editor's `--ring`, and the share dialog's rendered `background-color`. Confirmed the spec fails on the pre-fix code (expected `#4338ca`, received `#e8590c`) and passes after. typecheck + lint + 152 unit + 11 e2e + format + build green; ratchet raised (global statements/lines 34→35, `src/lib/**/*.ts` → 45/34/44/49); `pnpm size` 397.84 kB (−1.4 kB — a `<style>` element replaced an inline style attribute). |
| ENH-SEC-04   | 2026-07-16 | Re-tooled the advisory gate after npm retired the registry audit endpoints. `pnpm audit --prod` began failing with `ERR_PNPM_AUDIT_BAD_RESPONSE` — both the quick and fallback endpoints return 410 ("use the bulk advisory endpoint"), so CI was red on every branch for a reason unrelated to any change. pnpm hasn't migrated (reproduced on the pinned 10.30.3 and on 11.13.1), and `npm audit` can't stand in — it needs a package-lock.json a pnpm repo doesn't have (ENOLOCK). Replaced it with `osv-scanner` (pinned `v2.4.0`, run via its container) scanning `pnpm-lock.yaml`, i.e. the actually-resolved tree. Verified the gate can still fail: with the config removed it finds GHSA-67mh-4wv8-2f99 and exits 1; a synthetic vulnerable `lodash` lockfile also exits 1 (worth checking — osv-scanner auto-discovers `osv-scanner.toml` from the scanned dir, which can make a broken gate look green). Behaviour change: `--prod` scanned production deps only, while osv-scanner reads the whole lockfile, so a new dev-tool advisory now fails CI too — stricter, and accepted ones must be listed with a reason instead of silently skipped. Of the 2 moderate advisories GitHub was reporting, `markdown-it` (GHSA-6v5v-wf23-fmfq, dev-only via markdownlint-cli2) was **fixed** rather than ignored by bumping markdownlint-cli2 0.22.1 → 0.23.0, which ships markdown-it 14.2.0; only esbuild 0.18.20 remains ignored, and it's unfixable from here (better-auth → drizzle-kit → @esbuild-kit, deprecated and version-pinned) and never runs — the prod image migrates with the drizzle-orm runtime migrator. Dropped the now-dead `pnpm.auditConfig.ignoreGhsas` from package.json: nothing reads it any more (pnpm 11 warns it's ignored outright), and leaving it would imply protection that isn't there.                            |
