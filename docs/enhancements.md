# BragBit — Enhancement Backlog (side plan)

A companion to [PLAN.md](../PLAN.md). PLAN tracks the forward feature **phases** (Phase 10 = hosted
multi-tenant, Phase 11 = v2). This is the cross-cutting backlog of **quality, tech-debt, testing,
security, performance, and polish** work surfaced during the v0.1.0 build and the post-release code
review (2026-06-16). Items here are independently pickable; they don't block a release on their own.

**Priority:** P1 (do soon) · P2 (should) · P3 (nice-to-have).
**Effort:** S (< ½ day) · M (½–2 days) · L (> 2 days).

## A. Code quality & tech debt

| ID           | Pri      | Effort | Enhancement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Why                                                                                                                                                                                  |
| ------------ | -------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ENH-CQ-01    | ✅ Fixed | S      | ~~Replace the `app/page.tsx` demo mockup (318 lines of hardcoded fake brags) with a redirect to `/dashboard` or a real landing page.~~ **Done 2026-06-16** — root `/` is a mode/session-aware redirect; the demo mockup is gone. (Row was stale; already in the Done ledger.)                                                                                                                                                                                                                                                                                                                                                                                            | The root `/` shows fabricated data to real users — the biggest smell in the codebase.                                                                                                |
| ENH-CQ-02    | ✅ Fixed | S      | ~~Bump `tsconfig` `target` ES2017 → ES2022.~~ **Done 2026-06-20** — now `ES2022` (`lib` was already `esnext`, so type globals are unchanged); no bundle change, since Next compiles via SWC from Browserslist, not the tsconfig `target`.                                                                                                                                                                                                                                                                                                                                                                                                                                | Stale create-next-app default; Node 22 / modern browsers support far newer output.                                                                                                   |
| ENH-CQ-03    | P2       | M      | Decompose the largest client components (`brag-editor` 288, `share-dialog` 265) into focused sub-components.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Upper bound of comfortable size; smaller pieces read and test better.                                                                                                                |
| ENH-CQ-04    | ✅ Fixed | S      | ~~Move domain logic (the `beforeDelete` workspace-reaping) out of `lib/auth/index.ts` into a feature module.~~ **Done 2026-06-20** — extracted to `src/features/account/deletion.ts` (`cleanupUserStorage`); the `deleteUser` beforeDelete hook now just calls it (landed with the ENH-CQ-08 fix).                                                                                                                                                                                                                                                                                                                                                                       | The auth config object carries real business logic inline.                                                                                                                           |
| ENH-CQ-05    | P2       | M      | Create more bundle-budget headroom (now **398.37 / 400 kB**, measured 2026-06-20).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | ~1.6 kB of slack now (was ~3.8 at the `/` cleanup; drifted as enhancements landed) — tight; audit client JS or set the limit deliberately.                                           |
| ENH-CQ-06    | P3       | S      | Skip the guard double-redirect — send a signed-in guard / `/admin` failure straight to `/dashboard` instead of `/` (which re-dispatches).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | A non-admin hitting `/admin` now does `/admin → / → /dashboard` (two 307s) — harmless, but worth tidying when `/` gains the hosted-landing branch (Phase 10 / v1.1).                 |
| ENH-CQ-07 🐞 | ✅ Fixed | S      | ~~**BUG:** a signed-in but **workspace-less** session (e.g. a removed member who still has an account) ping-pongs `/dashboard → / → /dashboard` into `ERR_TOO_MANY_REDIRECTS`.~~ **Fixed 2026-06-19** — added a terminal `/no-workspace` page and pointed `requireWorkspace`'s two failure redirects at it (was `/`).                                                                                                                                                                                                                                                                                                                                                    | Found + fixed in org testing 2026-06-19 (TC-MEM-04). Access was always denied (no content leak); the bug was the infinite-loop failure mode. See Done ledger.                        |
| ENH-CQ-08 🐞 | ✅ Fixed | M      | ~~**BUG:** deleting a user account orphaned their uploaded attachment **files** in storage — the row cascade (user → … and a dropped sole-member org → documents → brags → attachments) removes the DB rows, but the stored objects were never deleted, unlike `deleteDocument` which purges them.~~ **Fixed 2026-06-20** — the `deleteUser` beforeDelete hook now calls `cleanupUserStorage` (`src/features/account/deletion.ts`), which purges the avatar + every orphaned attachment object (the user's own docs across workspaces, plus all docs in a dropped sole-member workspace) before the cascade. DB-gated regression test added to `object-cleanup.test.ts`. | Found in TC-ACCT-06 testing 2026-06-20. Storage-space leak only — orphaned files are unreachable (the file route 404s once the rows are gone), not a data exposure. See Done ledger. |

## B. Testing

| ID          | Pri      | Effort | Enhancement                                                                                                                                                                                                                                                                                                                 | Why                                                                                                 |
| ----------- | -------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| ENH-TEST-01 | ✅ Fixed | M      | ~~e2e for the core flow: capture → timeline → share (+ password) → export.~~ **Done 2026-06-20** — `tests/e2e/core-flow.spec.ts` drives the full journey (create doc → quick-add a win → timeline → share + password → logged-out unlock incl. wrong-password reject → Markdown export).                                    | Today only `admin-gating` + `home` exist; core flows had no e2e — the biggest gap. See Done ledger. |
| ENH-TEST-02 | ✅ Fixed | M      | ~~e2e for the setup wizard (both modes) + invitation accept.~~ **Done 2026-06-20** — `tests/e2e/invitation.spec.ts` (real sign-up → accept → membership; CI gained a Mailpit service) and `tests/e2e-setup/setup.spec.ts` (first-run wizard, both modes, via a dedicated `playwright.setup.config.ts` + isolated empty DB). | The first-run and onboarding paths are untested end to end. See Done ledger.                        |
| ENH-TEST-03 | ✅ Fixed | S      | ~~A documented `pnpm test:db` that runs the DB-gated suite against the dev database locally.~~ **Done 2026-06-20** — `pnpm test:db` (+ `test:db:coverage`), documented in CONTRIBUTING.                                                                                                                                     | They skip by default; easy local coverage would catch regressions before CI.                        |
| ENH-TEST-04 | ✅ Fixed | S      | ~~Add a QA checkbox to the PR template pointing at the [manual test plan](testing.md).~~ **Done 2026-06-20** — PR template links the manual test plan as a QA checkbox.                                                                                                                                                     | Prompts a manual pass on user-facing PRs.                                                           |
| ENH-TEST-05 | ✅ Fixed | M      | ~~Coverage reporting + a soft threshold in CI.~~ **Done 2026-06-20** — V8 coverage wired into the CI `database` job with a **ratchet** threshold in `vitest.config.ts` (fails on regression, not just soft). Baseline locked at the 2026-06-20 numbers.                                                                     | Visibility into what's actually exercised.                                                          |

## C. Security

| ID            | Pri      | Effort | Enhancement                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Why                                                                                                                                                                                                            |
| ------------- | -------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ENH-SEC-01    | P2       | M      | Add a `script-src` Content-Security-Policy with per-request nonces.                                                                                                                                                                                                                                                                                                                                                                                                                  | The CSP intentionally omits `script-src` today; nonces close the XSS gap (needs middleware).                                                                                                                   |
| ENH-SEC-02    | P2       | M      | Shared rate-limit store (Better Auth `secondaryStorage` / Redis-backed `lib/rate-limit`).                                                                                                                                                                                                                                                                                                                                                                                            | The in-memory limiter is single-process; multi-instance needs a shared store (Phase 10 prereq).                                                                                                                |
| ENH-SEC-03    | P3       | S      | Trusted-proxy client-IP configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accurate per-client rate-limiting behind the reference reverse proxy.                                                                                                                                          |
| ENH-SEC-04    | ✅ Fixed | S      | ~~`pnpm audit` in CI with an allowlist for the known dev-tool advisories.~~ **Done 2026-06-20** — CI runs `pnpm audit --prod`; the esbuild dev-server advisory (GHSA-67mh-4wv8-2f99, build-time only) is allowlisted via `pnpm.auditConfig.ignoreGhsas`. The check immediately caught a real HIGH prod advisory in nodemailer (≤9.0.0) → fixed by bumping to 9.0.1.                                                                                                                  | Catches new prod-dependency advisories without failing on the accepted build-tool ones. See Done ledger.                                                                                                       |
| ENH-SEC-05    | P2       | S      | A security-review pass + a dependency-update cadence before the repo goes public. **Review pass done 2026-06-20** (3-surface audit: auth/authz, XSS/injection/web, data/files/tokens) — findings logged as ENH-SEC-07/08; a flagged "OAuth email-collision takeover" was verified **not exploitable** (GitHub/Google only return an attacker-owned verified email). Dependency-cadence part still open (see ENH-INFRA-02).                                                           | Career data is sensitive; a deliberate review before public exposure.                                                                                                                                          |
| ENH-SEC-06 🐞 | ✅ Fixed | S      | ~~**BUG:** in the private (invitation-only) modes, `POST /api/auth/sign-up/email` is open — anyone reachable by the instance can create an account.~~ **Fixed 2026-06-19** — the catch-all auth route rejects the public `sign-up/email` path in non-hosted modes. `emailAndPassword.disableSignUp` couldn't be used (it's inside the sign-up handler, so it also blocks the setup wizard + invite-accept, which call `auth.api.signUpEmail` directly).                              | Found + fixed 2026-06-19. No data exposure (a self-signed-up user has no membership → `/no-workspace`), but it closes the "no public signup" gap + the verification-email relay-abuse vector. See Done ledger. |
| ENH-SEC-07    | ✅ Fixed | S      | ~~Share-password hashing uses `@node-rs/argon2` defaults (memoryCost 4 MiB, t=3) — below the OWASP Argon2id minimum (~19 MiB).~~ **Done 2026-06-20** — `src/features/share/actions.ts` now hashes with `{ memoryCost: 19456, timeCost: 2, parallelism: 1 }`; `verify` reads params from the encoded hash so existing hashes still validate.                                                                                                                                          | Cheaper offline cracking if the DB/backups leak. Found in the 2026-06-20 security review. See Done ledger.                                                                                                     |
| ENH-SEC-08    | ✅ Fixed | S      | ~~Operator-secret hardening: `BETTER_AUTH_SECRET` `.min(1)`→`.min(32)`; useSecureCookies; trustedOrigins.~~ **Done 2026-06-20** — `src/lib/env.ts` now requires a ≥32-char secret (CI placeholders + test docs updated); Better Auth `trustedOrigins: [APP_URL]` set. useSecureCookies intentionally **not** forced (Better Auth derives `Secure` from the `APP_URL` scheme, which correctly supports plain-http self-hosts) — instead `.env.example` documents using https in prod. | Prevents footguns that weaken session/cookie/HMAC security on a misconfigured self-host. Found in the 2026-06-20 security review. See Done ledger.                                                             |

## D. Performance

| ID          | Pri | Effort | Enhancement                                                      | Why                                                                                                  |
| ----------- | --- | ------ | ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| ENH-PERF-01 | P2  | L      | Timeline cursor pagination (month-windowed).                     | Deferred from Phase 5; the `(document_id, date)` index already exists. Matters at hundreds of brags. |
| ENH-PERF-02 | P3  | M      | `sharp`-downscaled thumbnails for image attachments and avatars. | Deferred Phase 4 refinement; the timeline currently serves full-res through the authorizing route.   |
| ENH-PERF-03 | P3  | S      | Review / enforce the Lighthouse CI budgets.                      | Keep Core Web Vitals honest as the app grows.                                                        |

## E. Infrastructure, CI & tooling

| ID           | Pri      | Effort | Enhancement                                                                                                                                                                                          | Why                                                                                                        |
| ------------ | -------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| ENH-INFRA-01 | ✅ Fixed | S      | ~~Bump GitHub Actions off Node 20 (`actions/checkout@v4`, `setup-node@v4`, etc.).~~ **Done 2026-06-20** — `checkout@v4→v5`, `setup-node@v4→v5`, `pnpm/action-setup@v4→v6` (all run on node24).       | Node 20 is being force-migrated to 24 on the runners; the deprecation warning was firing. See Done ledger. |
| ENH-INFRA-02 | ✅ Fixed | S      | ~~Add Renovate or Dependabot.~~ **Done 2026-06-20** — `.github/dependabot.yml`: weekly grouped updates for npm (pnpm), GitHub Actions, and the Docker base image, with Conventional-Commit prefixes. | Automated, reviewable dependency updates.                                                                  |
| ENH-INFRA-03 | P3       | S      | A dedicated `/api/health` endpoint for the container health check.                                                                                                                                   | Cleaner than hitting `/` (which renders the app).                                                          |
| ENH-INFRA-04 | P3       | L      | Optional headless-Chromium PDF service (`browserless`).                                                                                                                                              | Server-side PDF as an add-on; browser print is the deliberate v1 path.                                     |
| ENH-INFRA-05 | P3       | S      | Make `seed-demo` runnable inside the container, or document the local-only constraint.                                                                                                               | The script needs `dotenv` + `better-auth/crypto`, which aren't in the slim image.                          |

## F. UX & product polish

| ID        | Pri | Effort | Enhancement                                                                           | Why                                                                          |
| --------- | --- | ------ | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| ENH-UX-01 | P2  | L      | Dark mode — a designed ink-on-dark variant (not a mechanical invert).                 | Deferred from Phase 0; a real post-v1 enhancement.                           |
| ENH-UX-02 | P2  | M      | Accessibility pass (keyboard nav, focus management, ARIA on dialogs/forms, contrast). | No a11y audit has been done; important for an inclusive OSS tool.            |
| ENH-UX-03 | P3  | S      | Reduce the `no-img-element` eslint-disables where `next/image` can serve.             | 10 justified disables today; some non-authorizing images could be optimized. |
| ENH-UX-04 | P3  | M      | Entry content templates (action-verb scaffolds) to beat the blank page.               | Pulls a PLAN §11 item earlier; cheap capture-friction win.                   |
| ENH-UX-05 | P3  | M      | Streak + GitHub-style activity heatmap on the dashboard.                              | Drives logging cadence; pairs with the weekly reminders (PLAN §11).          |

## G. Tracked carryover

| ID        | Pri      | Effort | Enhancement                                                                                                       | Why                                                                          |
| --------- | -------- | ------ | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| ENH-CO-01 | ✅ Fixed | M      | ~~Member-removal export bundle (Markdown + JSON + attachments handed to a removed member).~~ **Done 2026-06-19.** | Phase 2 carryover, built on `features/export`. See Done ledger for delivery. |

## Tracked elsewhere (big phases — see PLAN.md)

- **Phase 10 — hosted multi-tenant (v1.1):** open signup, user-created orgs, the `/super` console,
  quota/abuse controls, the data-isolation test suite. See [PLAN.md §10](../PLAN.md).
- **Phase 11 — v2 backlog:** REST API + personal access tokens (and the
  [MCP connector](specs/mcp-connector.md) for in-assistant capture from Claude Desktop), a companion
  CLI (git-history brag extraction), SSO (OIDC/SAML), optional BYO-key AI, curated sharing,
  Markdown/Notion import. See [PLAN.md §11](../PLAN.md).

## Recommended order (highest leverage first)

1. **ENH-CQ-05 + ENH-CQ-02** — bundle headroom and the TS target (both cheap). _S–M_
2. **ENH-SEC-01 / ENH-SEC-02 / ENH-SEC-05** — before any public or hosted launch. _M_

## Done

| ID           | Done       | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ENH-CQ-01    | 2026-06-16 | Root `/` is now a mode/session-aware redirect; the demo mockup is gone (−2.5 kB).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ENH-CQ-07    | 2026-06-19 | Removed-member redirect loop. New terminal `src/app/no-workspace/page.tsx` + `requireWorkspace` now redirects no-workspace/no-membership callers there (not `/`). A workspace-less session lands on a "No workspace access" page with sign-out; valid users hitting it bounce to `/dashboard`. Verified: removed member sign-in → `/no-workspace` (no loop), sign-out recovers. typecheck + lint clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ENH-SEC-06   | 2026-06-19 | Closed the open public email/password sign-up in private (invitation-only) modes. The catch-all auth route (`src/app/api/auth/[...all]/route.ts`) returns 403 for the public `sign-up/email` path when `!isHosted()`; the setup wizard + invite-accept use `auth.api.signUpEmail` server-side and bypass the route, so they're unaffected. Verified live: public POST → 403 (no user); a fresh org setup + an invite-accept both still create their users. typecheck + lint clean.                                                                                                                                                                                                                                                                                                                                                                        |
| ENH-CO-01    | 2026-06-19 | Member-removal export bundle. `removeMember` now emails the removed member their full data (best-effort, after the purge) — a branded `MemberRemoved` mail with `bragbit-data.json` (every doc + brag, private included), a combined `bragbit-wins.md`, and the actual attachment files up to a 20 MB cap (oversized ones listed in the JSON). New `features/workspace/offboard.ts` (+ pure `offboard-bundle.ts` with a unit-tested size cap), `emails/member-removed.tsx`, and a `sendEmail` `attachments` passthrough. Live-verified against Mailpit: email + 3 attachments (json/md/pdf), JSON has 5 brags incl. private. typecheck + lint + tests green.                                                                                                                                                                                              |
| ENH-CQ-08    | 2026-06-20 | Account deletion no longer orphans attachment files. The `deleteUser` beforeDelete hook (`src/lib/auth/index.ts`) now calls `cleanupUserStorage` (`src/features/account/deletion.ts`): it collects every attachment object the deletion will orphan — the user's own documents across all workspaces, plus all documents in a sole-member workspace being dropped — and best-effort `getStorage().delete()`s them (alongside the avatar) before the row cascade. DB-gated regression test in `object-cleanup.test.ts` asserts the objects + avatar are purged and the sole-member workspace is dropped. Found in TC-ACCT-06 testing; storage-leak only (orphans were already unreachable). typecheck + lint + full test suite green (new test verified against the dev DB).                                                                               |
| ENH-CQ-04    | 2026-06-20 | Moved the `deleteUser` beforeDelete domain logic out of `lib/auth/index.ts` into `src/features/account/deletion.ts` (`cleanupUserStorage`); the auth config now just calls it. Landed with ENH-CQ-08.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ENH-TEST-01  | 2026-06-20 | Core-flow e2e. New `tests/e2e/core-flow.spec.ts` drives the product's main journey via the UI as the seeded e2e owner: create a document → quick-add a win → assert it on the timeline → open the Share dialog, create a link, set a password → in a fresh (logged-out) browser context open the share, get rejected with a wrong password, then unlock with the right one and see the win → export to Markdown and assert the download contains the document title + win. Reuses the `admin-gating` UI sign-in pattern; runs green in private-solo (the CI default mode). typecheck + lint + full e2e suite (4 tests) green.                                                                                                                                                                                                                             |
| ENH-INFRA-01 | 2026-06-20 | Moved CI off the deprecated Node 20 action runtime (GitHub is force-migrating Node-20 actions to Node 24; the warning was firing on every run). Bumped all 6 jobs in `.github/workflows/ci.yml`: `actions/checkout@v4→v5`, `actions/setup-node@v4→v5`, `pnpm/action-setup@v4→v6` — each confirmed `runs.using: node24` via the GitHub API. Left `node-version: 22` (the app's runtime, matching the `node:22` Dockerfile — unrelated to the action-runtime deprecation); `lychee-action`/`lighthouse-ci-action` weren't flagged (Docker-based / already node24). pnpm version still resolves from `packageManager: pnpm@10.30.3`, so the major bump doesn't change it.                                                                                                                                                                                    |
| ENH-SEC-07   | 2026-06-20 | Raised the share-password argon2 work factor to the OWASP Argon2id minimum. `setSharePassword` (`src/features/share/actions.ts`) now hashes with `{ memoryCost: 19456, timeCost: 2, parallelism: 1 }` instead of the 4 MiB `@node-rs/argon2` default; `verify` reads the cost params back from the encoded hash, so previously-set passwords keep working. Verified via the DB-gated `share/security.test.ts` and the core-flow e2e (set + unlock through the UI).                                                                                                                                                                                                                                                                                                                                                                                        |
| ENH-SEC-08   | 2026-06-20 | Operator-secret hardening from the security review. `BETTER_AUTH_SECRET` validation raised to `.min(32)` (`src/lib/env.ts`) — updated the 4 CI placeholder secrets and the two test-run doc comments to ≥32 chars to match. Set Better Auth `trustedOrigins: [APP_URL]` so legitimate same-origin/proxied requests pass the CSRF Origin check. Did NOT force `useSecureCookies`: Better Auth already derives the cookie `Secure` flag from the `APP_URL` scheme (correct for both https and plain-http self-hosts), so instead `.env.example` documents using https in production. typecheck + lint + full e2e (4) + DB-gated security tests (11) green.                                                                                                                                                                                                  |
| ENH-SEC-04   | 2026-06-20 | Added `pnpm audit --prod` to the CI `verify` job, with the accepted dev-tool advisory allowlisted in `package.json` (`pnpm.auditConfig.ignoreGhsas: ["GHSA-67mh-4wv8-2f99"]` — the esbuild dev-server CORS issue, which only reaches the tree via `better-auth → drizzle-kit → @esbuild-kit` and never runs in the standalone prod image, since `scripts/migrate.mjs` uses the drizzle-orm runtime migrator). The new check immediately caught a real HIGH advisory in nodemailer (≤9.0.0: file-read + SSRF in the delivered message, GHSA-p6gq-j5cr-w38f) — fixed by bumping nodemailer 8→9.0.1 (kept @types/nodemailer@8; createTransport/sendMail API unchanged). `pnpm audit --prod` now exits clean; typecheck + build + tests green.                                                                                                                |
| ENH-INFRA-02 | 2026-06-20 | Added `.github/dependabot.yml`: weekly grouped dependency updates for npm (pnpm, root), GitHub Actions, and the Docker base image. Minor/patch npm bumps group into one PR (majors stay separate); commit prefixes are Conventional (`chore`/`ci`) to satisfy commitlint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ENH-TEST-03  | 2026-06-20 | Added `pnpm test:db` (and `test:db:coverage`) — runs the full suite incl. the DB-gated server-action tests against the dev DB (`:5433`), documented in CONTRIBUTING. Closes the "skipped by default, no easy local run" gap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ENH-TEST-04  | 2026-06-20 | PR template (`.github/PULL_REQUEST_TEMPLATE.md`) now has a QA checkbox linking the manual test plan (shipped with the public-hygiene batch; backlog row reconciled).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ENH-TEST-05  | 2026-06-20 | Coverage reporting + a ratcheting CI gate. Added `@vitest/coverage-v8` (pinned to vitest 4.1.8) and a `coverage` block in `vitest.config.ts` (v8, lcov+json-summary+text-summary, `all:true`, schema/migrations/layout excluded). CI measures coverage in the `database` job (`pnpm test:coverage`, where DB-gated tests run so server actions count) and uploads the report. Thresholds are a ratchet locked to the 2026-06-20 baseline — global lines/stmts/funcs 13 / branches 9, with stricter floors for `src/lib/**/*.ts` (lines 37) and `src/features/**/*.ts` (lines 32). Enforcement verified (an impossible floor fails the run); the gate climbs as Layer 2/3 add tests.                                                                                                                                                                       |
| ENH-TEST-02  | 2026-06-20 | End-to-end coverage for onboarding. `tests/e2e/invitation.spec.ts` drives the real accept flow (logged-out invitee → Better Auth sign-up → accept → asserts membership by polling the DB; the CI e2e job gained a throwaway Mailpit SMTP service so the verification email sends). The setup wizard needs an EMPTY, mode-specific instance (it closes once a workspace exists, and `INSTANCE_MODE` is fixed at start), so it runs under a dedicated `playwright.setup.config.ts` against an isolated DB (`scripts/e2e-setup-db.mjs` provisions it; the spec truncates per attempt) on its own `next dev` port — invoked once per private mode by `pnpm test:e2e:setup` (sequential, so only one `next dev` is ever live). Verifies the wizard creates a personal (solo) vs organization (org) workspace and that the member surface exists only for orgs. |
| ENH-CQ-02    | 2026-06-20 | Bumped `tsconfig.json` `target` ES2017 → ES2022 (`lib` was already `esnext`, so available type globals are unchanged; the one semantic shift — `useDefineForClassFields` defaulting to `true` — is harmless here: the two storage classes (`S3Storage`, `LocalDiskStorage`) declare no class-field initializers and have no inheritance). No `src`/test changes. Verified: typecheck clean, `pnpm build` succeeds, fast suite (44) + the DB-gated coverage suite green at/above the ratchet floors, and `pnpm size` is identical to the ES2017 build (398.37 kB ≤ 400 kB) — confirming Next ships SWC output targeted from Browserslist, not the tsconfig `target`.                                                                                                                                                                                       |
