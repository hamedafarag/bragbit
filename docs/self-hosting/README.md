# Self-hosting BragBit

> **Status:** stub — full guides land in Phase 9.

BragBit is Docker-first; the reference deployment target is **Dokploy on a private VPS**, with
generic Docker Compose and a Vercel + managed-Postgres variant as alternatives.

Planned guides:

- `dokploy.md` — the reference deployment
- `docker-compose.md` — generic Compose
- `vercel.md` — Vercel + Neon / managed Postgres
- Backup/restore & upgrade notes

Until these land, see [`.env.example`](../../.env.example) and [configuration.md](../configuration.md).
